---
phase: 07-auth-verify-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipcard/package.json
  - README.md
  - USAGE.md
  - shipcard/docs/mcp-config.md
  - shipcard-worker/.dev.vars
autonomous: false

user_setup:
  - service: github-oauth
    why: "Device Flow must be enabled on the OAuth App"
    dashboard_config:
      - task: "Verify Device Flow checkbox is enabled"
        location: "github.com/settings/developers -> ShipCard OAuth App -> General"
  - service: cloudflare-worker
    why: "Local testing requires real secrets in .dev.vars"
    env_vars:
      - name: GITHUB_CLIENT_SECRET
        source: "github.com/settings/developers -> ShipCard OAuth App -> Client secrets"
      - name: TOKEN_SECRET
        source: "Any 32-char hex string (e.g. openssl rand -hex 16)"

must_haves:
  truths:
    - "npm package name is 'shipcard' (unscoped) in package.json"
    - "All docs reference 'shipcard' not '@jjaimealeman/shipcard'"
    - "npx shipcard summary works without -p flag (after publish)"
    - "MCP config docs still use -p shipcard for shipcard-mcp binary"
    - "shipcard login completes GitHub device flow end-to-end"
  artifacts:
    - path: "shipcard/package.json"
      provides: "Unscoped package name"
      contains: "\"name\": \"shipcard\""
    - path: "README.md"
      provides: "Updated install/npx commands"
      contains: "npx shipcard summary"
    - path: "USAGE.md"
      provides: "Updated MCP config + npx section"
      contains: "-p shipcard"
    - path: "shipcard/docs/mcp-config.md"
      provides: "Updated MCP config snippets"
      contains: "-p shipcard"
  key_links:
    - from: "README.md"
      to: "shipcard/package.json"
      via: "package name consistency"
      pattern: "npm install -g shipcard"
    - from: "USAGE.md MCP config"
      to: "shipcard/package.json bin entries"
      via: "npx -p shipcard shipcard-mcp"
      pattern: "-p shipcard.*shipcard-mcp"
---

<objective>
Rename npm package from @jjaimealeman/shipcard to unscoped shipcard, update all documentation to match, and verify the GitHub OAuth device flow works end-to-end.

Purpose: This is the final verification/polish phase before the project is shippable. The unscoped name simplifies npx usage (the main entry point for new users), and OAuth verification confirms the full login->sync->card pipeline works.

Output: Updated package.json, README.md, USAGE.md, docs/mcp-config.md with unscoped name references. Verified OAuth flow (or documented fix if something was broken).
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auth-verify-docs/07-RESEARCH.md
@.planning/phases/07-auth-verify-docs/07-CONTEXT.md
@shipcard/package.json
@README.md
@USAGE.md
@shipcard/docs/mcp-config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package to unscoped + update all docs</name>
  <files>
    shipcard/package.json
    README.md
    USAGE.md
    shipcard/docs/mcp-config.md
  </files>
  <action>
    1. In shipcard/package.json, change "name" from "@jjaimealeman/shipcard" to "shipcard". No other changes to package.json.

    2. In README.md Quick Start section:
       - Change `npm install -g @jjaimealeman/shipcard` to `npm install -g shipcard`
       - Change `npx -p @jjaimealeman/shipcard shipcard summary` to `npx shipcard summary`

    3. In README.md MCP Config section:
       - Change `"-p", "@jjaimealeman/shipcard"` to `"-p", "shipcard"`
       (Keep -p flag — shipcard-mcp binary does not match package name "shipcard")

    4. In USAGE.md MCP Setup section:
       - Change `"-p", "@jjaimealeman/shipcard"` to `"-p", "shipcard"`

    5. In shipcard/docs/mcp-config.md:
       - Replace ALL occurrences of `@jjaimealeman/shipcard` with `shipcard`
       - This includes Claude Code config, Cursor config, and the global install section
       - The global install example: `npm install -g shipcard`

    6. After all edits, run: grep -r "@jjaimealeman" /home/jaime/www/_github/SaaS --include="*.md" --include="*.json" --exclude-dir=node_modules
       This MUST return zero results. If any remain, fix them.

    IMPORTANT: Do NOT remove the -p flag from MCP configs. The MCP binary is "shipcard-mcp" which does not match the package name "shipcard", so -p is still required for npx to find the right package.
  </action>
  <verify>
    Run: grep -r "@jjaimealeman" /home/jaime/www/_github/SaaS --include="*.md" --include="*.json" --exclude-dir=node_modules
    Must return zero results.

    Run: grep "npx shipcard summary" /home/jaime/www/_github/SaaS/README.md
    Must match (confirms unscoped npx command was inserted).

    Run: cd /home/jaime/www/_github/SaaS/shiplog && npm run build
    Build must succeed with the renamed package.
  </verify>
  <done>
    - package.json name is "shipcard" (unscoped)
    - Zero occurrences of "@jjaimealeman" in any .md or .json file (excluding node_modules)
    - CLI npx examples use "npx shipcard <cmd>" (no -p)
    - MCP npx examples use "-p shipcard" (with -p, because binary name differs)
    - Build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify OAuth device flow end-to-end</name>
  <what-built>
    All code for OAuth login, sync, and card serving was built in Phases 4-5. This checkpoint verifies the full round-trip works with the real GitHub OAuth App.

    Before testing, ensure:
    1. GitHub OAuth App has "Device Flow" enabled (github.com/settings/developers -> ShipCard app)
    2. shipcard-worker/.dev.vars has real (uncommented) values for GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, TOKEN_SECRET
  </what-built>
  <how-to-verify>
    **Local test (recommended first):**

    1. Populate shipcard-worker/.dev.vars with real secrets:
       ```
       GITHUB_CLIENT_ID="Ov23lijo8A2inPwKNCnx"
       GITHUB_CLIENT_SECRET="<from-github-settings>"
       TOKEN_SECRET="<any-32-char-hex>"
       ```

    2. Start local Worker:
       ```
       cd /home/jaime/www/_github/SaaS/shiplog-worker && npx wrangler dev
       ```

    3. Temporarily set workerUrl for local testing:
       Edit ~/.shipcard/config.json -> add "workerUrl": "http://localhost:8787"

    4. Build CLI and test login:
       ```
       cd /home/jaime/www/_github/SaaS/shiplog && npm run build
       node dist/cli/index.js login
       ```
       Expected: browser opens github.com/login/device, you enter code, CLI prints "Logged in as <username>"

    5. Test sync:
       ```
       node dist/cli/index.js sync --confirm
       ```
       Expected: "Card synced! View at: http://localhost:8787/u/<username>"

    6. Fetch card:
       ```
       curl http://localhost:8787/u/<username>
       ```
       Expected: SVG content returned

    **Live test (after local passes):**

    7. Remove workerUrl override from ~/.shipcard/config.json (or set to "https://shipcard.dev")
    8. Ensure Worker is deployed: curl https://shipcard.dev/ (should return JSON health check)
    9. If not deployed: cd shiplog-worker && npx wrangler deploy
    10. Run login + sync against live worker
    11. Visit https://shipcard.dev/u/<username> in browser — should show real card

    **Troubleshooting (common failures):**

    - **"device_flow_disabled" error:** Go to github.com/settings/developers -> ShipCard OAuth App -> enable "Device Flow" checkbox -> save -> retry
    - **Client ID mismatch:** Verify GITHUB_CLIENT_ID in .dev.vars matches the OAuth App's Client ID shown in GitHub settings
    - **Auth exchange fails (500):** Check that GITHUB_CLIENT_SECRET in .dev.vars matches the OAuth App's secret
    - **Sync fails with 401:** Token may have expired or been invalidated — run `login` again
    - **Card not found after sync:** Check Worker logs (wrangler tail) for KV write errors

    If any issue requires a code fix, describe the error and Claude will fix it before retesting.
  </how-to-verify>
  <resume-signal>Type "approved" if full flow works, or describe what broke (include error message) and I will fix it in this session</resume-signal>
</task>

</tasks>

<verification>
1. `grep -r "@jjaimealeman" --include="*.md" --include="*.json" --exclude-dir=node_modules` returns nothing
2. `cd shiplog && npm run build` succeeds
3. README.md Quick Start shows `npx shipcard summary` (no -p flag)
4. All MCP config blocks show `"-p", "shipcard"` (with -p flag)
5. OAuth login -> sync -> card fetch works end-to-end (human verified)
</verification>

<success_criteria>
- Package name is "shipcard" (unscoped) in package.json
- All documentation references updated consistently
- GitHub OAuth device flow completes successfully (login -> sync -> card visible)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-verify-docs/07-01-SUMMARY.md`
</output>
