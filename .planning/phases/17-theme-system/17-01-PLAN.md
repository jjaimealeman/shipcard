---
phase: 17-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipcard-worker/src/svg/themes/curated.ts
  - shipcard-worker/src/svg/themes/contrast.ts
  - shipcard-worker/src/svg/themes/index.ts
  - shipcard-worker/src/svg/renderer.ts
  - shipcard-worker/src/svg/index.ts
autonomous: true

must_haves:
  truths:
    - "9 curated themes exist as typed ThemeColors objects with verified hex values"
    - "resolveTheme() accepts a named curated theme string and returns the correct palette"
    - "Legacy style+theme resolution still works (backward compat for existing embeds)"
    - "Contrast validator correctly computes WCAG 2.1 relative luminance and 3:1 ratio check"
    - "renderSvg() can accept a ThemeColors directly (not just style+theme pair)"
  artifacts:
    - path: "shipcard-worker/src/svg/themes/curated.ts"
      provides: "9 curated theme palettes + CuratedThemeName type + CURATED_THEME_NAMES list"
      exports: ["CURATED_THEMES", "CuratedThemeName", "CURATED_THEME_NAMES"]
    - path: "shipcard-worker/src/svg/themes/contrast.ts"
      provides: "WCAG contrast ratio calculator + BYOT validation"
      exports: ["contrastRatio", "validateByotContrast", "ByotColors", "ContrastError"]
    - path: "shipcard-worker/src/svg/themes/index.ts"
      provides: "Updated registry supporting both legacy and curated theme resolution"
      exports: ["resolveTheme", "resolveCuratedTheme", "ThemeColors", "CuratedThemeName", "CURATED_THEME_NAMES"]
    - path: "shipcard-worker/src/svg/renderer.ts"
      provides: "Updated RenderOptions accepting ThemeColors directly"
    - path: "shipcard-worker/src/svg/index.ts"
      provides: "Updated CardOptions with curated theme + BYOT color support"
  key_links:
    - from: "shipcard-worker/src/svg/themes/curated.ts"
      to: "shipcard-worker/src/svg/themes/types.ts"
      via: "ThemeColors import"
      pattern: "import.*ThemeColors.*types"
    - from: "shipcard-worker/src/svg/themes/index.ts"
      to: "shipcard-worker/src/svg/themes/curated.ts"
      via: "CURATED_THEMES import for resolution"
      pattern: "import.*CURATED_THEMES.*curated"
    - from: "shipcard-worker/src/svg/renderer.ts"
      to: "shipcard-worker/src/svg/themes/index.ts"
      via: "resolveTheme or direct ThemeColors"
      pattern: "resolveTheme|ThemeColors"
---

<objective>
Create the curated theme palette registry, WCAG contrast validator, and update the renderer pipeline to support named themes alongside the legacy style+theme system.

Purpose: This is the foundation layer -- curated palettes, contrast checking, and renderer plumbing that the card route (Plan 02) and dashboard configurator (Plan 03) will build on.
Output: 2 new files (curated.ts, contrast.ts), 3 updated files (themes/index.ts, renderer.ts, svg/index.ts)
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-theme-system/17-CONTEXT.md
@.planning/phases/17-theme-system/17-RESEARCH.md

# Key source files to read before modifying
@shipcard-worker/src/svg/themes/types.ts
@shipcard-worker/src/svg/themes/index.ts
@shipcard-worker/src/svg/themes/github.ts
@shipcard-worker/src/svg/renderer.ts
@shipcard-worker/src/svg/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create curated theme palettes and contrast validator</name>
  <files>
    shipcard-worker/src/svg/themes/curated.ts
    shipcard-worker/src/svg/themes/contrast.ts
  </files>
  <action>
    Create `curated.ts` with all 9 curated theme palettes as a `Record<CuratedThemeName, ThemeColors>`:

    - Export type `CuratedThemeName` as union: "catppuccin" | "dracula" | "tokyo-night" | "nord" | "gruvbox" | "solarized-dark" | "solarized-light" | "one-dark" | "monokai"
    - Export const `CURATED_THEMES` with all 9 palettes. Each palette has 7 fields (bg, border, title, text, value, icon, footer). Use the EXACT hex values from 17-RESEARCH.md (verified from official sources). For all themes: `value = title`, `footer = text`.
    - Export const `CURATED_THEME_NAMES` as `Object.keys(CURATED_THEMES) as CuratedThemeName[]`

    Create `contrast.ts` with WCAG 2.1 contrast validation:

    - Internal `linearize(v: number): number` helper for sRGB linearization
    - Internal `relativeLuminance(hex: string): number` — parses hex (with or without #), computes WCAG luminance
    - Export `contrastRatio(hex1: string, hex2: string): number` — returns ratio between 1 and 21
    - Export type `ByotColors = { bg: string; title: string; text: string; icon: string; border: string }`
    - Export type `ContrastError = { field: string; message: string }`
    - Export `validateByotContrast(colors: ByotColors): ContrastError[]` — checks title/text/icon/border each against bg. Uses MIN_RATIO = 3.0 (WCAG 1.4.11, NOT 4.5:1). Returns array of field-specific error messages. Empty array = pass.
    - Export `isValidHex(hex: string): boolean` — validates 6-char hex with or without #

    Use the exact implementation patterns from 17-RESEARCH.md. No external dependencies. Function-object style (no classes).
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shipcard-worker && npx tsc --noEmit` — zero errors.
    Verify all 9 theme objects have exactly 7 fields each.
    Verify contrast.ts exports match the specified API.
  </verify>
  <done>
    9 curated themes exist with verified hex values. Contrast validator implements WCAG 3:1 check with field-specific error messages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update theme registry and renderer for curated theme support</name>
  <files>
    shipcard-worker/src/svg/themes/index.ts
    shipcard-worker/src/svg/renderer.ts
    shipcard-worker/src/svg/index.ts
  </files>
  <action>
    Update `themes/index.ts`:

    - Import `CURATED_THEMES`, `CuratedThemeName`, `CURATED_THEME_NAMES` from `./curated.js`
    - Re-export `CuratedThemeName`, `CURATED_THEME_NAMES` for external consumers
    - Re-export `contrastRatio`, `validateByotContrast`, `ByotColors`, `ContrastError`, `isValidHex` from `./contrast.js`
    - Add `resolveCuratedTheme(name: string): ThemeColors | null` — looks up name in CURATED_THEMES. Returns null if not found.
    - Keep existing `resolveTheme(style, theme)` completely unchanged for backward compat
    - Add `resolveThemeV2(params: { theme?: string; style?: string; legacyTheme?: string }): ThemeColors` that:
      1. If `theme` is in CURATED_THEME_NAMES, return curated palette
      2. If `theme` is "dark" or "light", treat as legacy: return `resolveTheme(style ?? "github", theme)`
      3. Otherwise fall back to `resolveTheme(style ?? "github", "dark")` (default)
      This handles backward compat where old URLs use `?theme=dark` and new URLs use `?theme=catppuccin`

    Update `renderer.ts`:

    - Add `ThemeColors` to the import from themes/index.js (if not already there)
    - Update `RenderOptions` to accept an optional `colors?: ThemeColors` field
    - In `renderSvg()`: if `options.colors` is provided, use it directly. Otherwise resolve via `resolveTheme(style, theme)` as before. This allows the card route to pass pre-resolved colors without the renderer needing to know about curated vs BYOT vs legacy.

    Update `svg/index.ts`:

    - Add `ThemeColors` to imports from themes
    - Add optional `colors?: ThemeColors` to `CardOptions` interface
    - In `renderCard()`: pass `options.colors` through to `renderSvg()` via the options object
    - Export `CuratedThemeName`, `CURATED_THEME_NAMES` from themes for the card route to use

    IMPORTANT: Do NOT remove or change any existing exports. The old `StyleName`, `ThemeName`, `resolveTheme` must all continue to work identically.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shipcard-worker && npx tsc --noEmit` — zero errors.
    Confirm existing `resolveTheme("github", "dark")` still works.
    Confirm `resolveCuratedTheme("catppuccin")` returns the catppuccin palette.
    Confirm `resolveCuratedTheme("invalid")` returns null.
  </verify>
  <done>
    Theme registry supports both legacy style+theme and new curated theme names. Renderer accepts pre-resolved ThemeColors. All existing exports preserved.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in shipcard-worker passes with zero errors
2. All 9 curated theme names are resolvable via `resolveCuratedTheme()`
3. Legacy `resolveTheme("github", "dark")` returns githubDark (unchanged behavior)
4. `validateByotContrast()` returns errors for low-contrast pairs and empty array for valid pairs
5. `renderSvg()` accepts both old `{ style, theme }` and new `{ colors }` options
</verification>

<success_criteria>
- 2 new files created: curated.ts (9 palettes), contrast.ts (WCAG validator)
- 3 files updated: themes/index.ts, renderer.ts, svg/index.ts
- Zero breaking changes to existing theme resolution or rendering
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/17-theme-system/17-01-SUMMARY.md`
</output>
