---
phase: 18-stripe-subscriptions
plan: 04
type: execute
wave: 3
depends_on: ["18-02", "18-03"]
files_modified:
  - shipcard-worker/src/routes/dashboard.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard shows PRO badge in header when user has active subscription"
    - "Dashboard shows payment-failed banner with countdown when payment_failed_at is set"
    - "Dashboard shows billing section with plan status, next billing date, and Manage Subscription link"
    - "Free users see upgrade prompts at BYOT section (and future PRO feature touchpoints)"
    - "Upgrade prompt shows feature list and both pricing options ($2/mo, $20/yr)"
  artifacts:
    - path: "shipcard-worker/src/routes/dashboard.ts"
      provides: "Billing UI section, PRO badge, payment banner, upgrade prompts"
      contains: "billing"
  key_links:
    - from: "shipcard-worker/src/routes/dashboard.ts"
      to: "shipcard-worker/src/db/subscriptions.ts"
      via: "import { getSubscription }"
      pattern: "getSubscription"
    - from: "shipcard-worker/src/routes/dashboard.ts"
      to: "/billing/checkout"
      via: "form action pointing to checkout endpoint"
      pattern: "/billing/checkout"
---

<objective>
Add billing UI to the dashboard: PRO badge, payment-failed banner, billing/settings section, and upgrade prompts at PRO feature touchpoints.

Purpose: Users need to see their subscription status, manage it, and free users need clear upgrade paths. This is the UI that makes the billing backend visible.
Output: Updated dashboard.ts with billing section, PRO badge, payment banner, and upgrade prompts
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-stripe-subscriptions/18-CONTEXT.md
@.planning/phases/18-stripe-subscriptions/18-RESEARCH.md
@.planning/phases/18-stripe-subscriptions/18-01-SUMMARY.md
@.planning/phases/18-stripe-subscriptions/18-02-SUMMARY.md
@.planning/phases/18-stripe-subscriptions/18-03-SUMMARY.md
@shipcard-worker/src/routes/dashboard.ts
@shipcard-worker/src/db/subscriptions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subscription data injection + PRO badge + payment banner</name>
  <files>shipcard-worker/src/routes/dashboard.ts</files>
  <action>
    1. Import `getSubscription` from `../db/subscriptions.js` at the top of the file.

    2. In the GET handler (bottom of file), after the existing `isUserPro` call:
       - Fetch subscription details: `const subscription = await getSubscription(c.env.DB, username)`
       - Inject subscription data into HTML via new placeholders:
         - `__SUBSCRIPTION_STATUS__` → subscription?.status || 'free'
         - `__PAYMENT_FAILED__` → subscription?.payment_failed_at ? 'true' : 'false'
         - `__PERIOD_END__` → subscription?.current_period_end ? String(subscription.current_period_end) : '0'

    3. Add PRO badge to dashboard header:
       - In the header/nav area of DASHBOARD_HTML, add a conditional PRO badge:
         `<span x-show="isPro" class="pro-badge">PRO</span>`
       - Style: small orange pill badge (background: var(--orange), color: white, font-size: 0.7rem, padding: 2px 8px, border-radius: 12px, font-weight: 700, margin-left: 8px)

    4. Add payment-failed banner:
       - Below the header, before the main content area:
         ```html
         <div x-show="paymentFailed" class="payment-banner" style="display:none">
           <span>Payment failed. Please update your payment method to keep PRO features.</span>
           <form method="POST" action="/billing/portal" style="display:inline">
             <button type="submit" class="banner-btn">Update Payment</button>
           </form>
         </div>
         ```
       - Style: background #3d1f1f (dark red tint), border: 1px solid #7c3030, padding 12px 16px, border-radius var(--radius), margin-bottom 16px, color var(--fg)
       - banner-btn: background var(--orange), color white, border none, padding 4px 12px, border-radius 4px, cursor pointer

    5. Add Alpine data for subscription state:
       - In the x-data init object, add: `paymentFailed: __PAYMENT_FAILED__`, `subscriptionStatus: '__SUBSCRIPTION_STATUS__'`, `periodEnd: __PERIOD_END__`

    6. Enhance the existing BYOT lock overlay (the `byot-locked` div that shows for non-PRO users):
       - Replace the existing simple lock message with a mini feature card:
         ```html
         <div class="upgrade-card">
           <h4>Unlock PRO</h4>
           <ul>
             <li>Custom theme colors (BYOT)</li>
             <li>Custom card slugs</li>
             <li>AI-powered insights</li>
             <li>Priority cache refresh</li>
           </ul>
           <div class="upgrade-pricing">
             <form method="POST" action="/billing/checkout">
               <input type="hidden" name="interval" value="month">
               <button type="submit" class="upgrade-btn">$2/month</button>
             </form>
             <form method="POST" action="/billing/checkout">
               <input type="hidden" name="interval" value="year">
               <button type="submit" class="upgrade-btn upgrade-annual">$20/year <span class="save-tag">Save 17%</span></button>
             </form>
           </div>
         </div>
         ```
       - Style upgrade-card: background var(--surface), border 1px solid var(--border), padding 20px, border-radius var(--radius), text-align center
       - upgrade-btn: background var(--orange), color white, border none, padding 8px 20px, border-radius 6px, cursor pointer, font-weight 600, width 100%, margin-top 8px
       - upgrade-annual: background transparent, border 1px solid var(--orange), color var(--orange)
       - save-tag: font-size 0.7rem, background var(--green), color white, padding 1px 6px, border-radius 4px, margin-left 4px

    NOTE: The checkout forms use POST to /billing/checkout. These will need auth (the user's bearer token). Since the dashboard is a public page, the forms need to include the auth token. The simplest approach: if the dashboard detects the user is logged in (via a cookie or the existing auth flow), include a hidden input with the token. However, since the current auth model uses bearer tokens from the CLI (not browser cookies), the checkout button should instead use JavaScript to POST with the token from localStorage/sessionStorage if available, OR redirect to a login-then-checkout flow.

    PRAGMATIC APPROACH: Since this is the first browser-authenticated flow and the existing dashboard is public (no login), use a simple approach:
    - The upgrade buttons link to `/billing/checkout?interval=month` (GET redirect that starts GitHub OAuth, then creates checkout). BUT this adds auth complexity.
    - SIMPLEST: Make the upgrade button a link that goes to `/billing/checkout?interval=month` and have the billing route handle the GitHub OAuth flow inline (redirect to GitHub -> callback -> checkout). This defers to how the dashboard auth evolves.
    - FOR NOW: Use `<a>` tags instead of forms. The billing/checkout route can handle GET requests and redirect through OAuth if needed. The executor should decide the cleanest auth mechanism given the current codebase state.
  </action>
  <verify>
    Run `cd shipcard-worker && npx tsc --noEmit` — compiles cleanly.
    Search for "pro-badge" in dashboard.ts — should exist.
    Search for "payment-banner" in dashboard.ts — should exist.
    Search for "upgrade-card" in dashboard.ts — should exist.
  </verify>
  <done>
    Dashboard shows PRO badge for subscribers, payment-failed banner when payment is overdue, and upgrade prompt cards at BYOT touchpoint with both monthly and annual pricing options.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add billing/settings section to dashboard</name>
  <files>shipcard-worker/src/routes/dashboard.ts</files>
  <action>
    Add a new "Billing" section to the dashboard HTML, placed AFTER the Theme Configurator section.

    The section should show different content based on subscription status:

    **For PRO users (x-show="isPro"):**
    ```html
    <section class="dashboard-section" x-show="isPro">
      <h2>Billing</h2>
      <div class="billing-info">
        <div class="billing-row">
          <span class="billing-label">Plan</span>
          <span class="billing-value">PRO <span class="pro-badge-sm">Active</span></span>
        </div>
        <div class="billing-row" x-show="periodEnd > 0">
          <span class="billing-label">Next billing date</span>
          <span class="billing-value" x-text="new Date(periodEnd * 1000).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })"></span>
        </div>
        <div class="billing-actions">
          <a href="/billing/portal" class="billing-link">Manage Subscription</a>
        </div>
      </div>
    </section>
    ```

    **For free users (x-show="!isPro"):**
    ```html
    <section class="dashboard-section" x-show="!isPro" style="display:none">
      <h2>Plan</h2>
      <div class="billing-info">
        <div class="billing-row">
          <span class="billing-label">Current plan</span>
          <span class="billing-value">Free</span>
        </div>
        <p class="billing-upgrade-hint">Upgrade to PRO for custom themes, slugs, and AI insights.</p>
      </div>
    </section>
    ```

    Styles:
    - billing-info: background var(--surface), border 1px solid var(--border), border-radius var(--radius), padding 16px
    - billing-row: display flex, justify-content space-between, padding 8px 0, border-bottom 1px solid var(--border)
    - billing-row:last-child: border-bottom none
    - billing-label: color var(--mid)
    - billing-value: color var(--fg), font-weight 600
    - billing-link: color var(--orange), text-decoration none
    - billing-link:hover: text-decoration underline
    - pro-badge-sm: font-size 0.65rem, background var(--green), color white, padding 1px 6px, border-radius 4px
    - billing-upgrade-hint: color var(--mid), font-size 0.9rem, margin-top 12px

    NOTE: The "Manage Subscription" link uses a simple GET to /billing/portal. The billing route should handle this as a GET that creates a portal session. Since portal also needs to know WHO the user is, this has the same auth consideration as checkout. For now, use an `<a>` tag — the billing route handler will need to resolve the user (from cookie, session, or OAuth redirect).
  </action>
  <verify>
    Run `cd shipcard-worker && npx tsc --noEmit` — compiles cleanly.
    Search for "Billing" section in dashboard.ts — should exist.
    Search for "Manage Subscription" in dashboard.ts — should exist.
  </verify>
  <done>
    Dashboard has a Billing section showing plan status and next billing date for PRO users, and a simple "Free" indicator with upgrade hint for free users. Manage Subscription links to Stripe Customer Portal.
  </done>
</task>

</tasks>

<verification>
- `cd shipcard-worker && npx tsc --noEmit` passes
- Dashboard HTML contains: pro-badge, payment-banner, upgrade-card with pricing, billing section
- PRO users see: badge, billing section with status + manage link
- Free users see: upgrade prompt at BYOT with feature list + pricing
- Payment-failed users see: banner with update payment button
- All pricing matches: $2/month and $20/year
</verification>

<success_criteria>
The dashboard visually communicates subscription status (free/PRO/payment-failed) and provides clear upgrade paths with both pricing options. PRO users can access subscription management.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-subscriptions/18-04-SUMMARY.md`
</output>
