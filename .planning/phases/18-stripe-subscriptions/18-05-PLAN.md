---
phase: 18-stripe-subscriptions
plan: 05
type: execute
wave: 4
depends_on: ["18-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "POST /webhook/stripe returns 200 for valid Stripe webhook events and persists subscription state to D1"
    - "GET /billing/checkout redirects to GitHub OAuth, then to Stripe Checkout with correct price and metadata"
    - "GET /billing/portal redirects to GitHub OAuth, then to Stripe Customer Portal for the authenticated user"
    - "Dashboard shows PRO badge, billing section, and Manage Subscription link for PRO users"
    - "Dashboard shows upgrade prompt with $2/mo and $20/yr pricing at BYOT section for free users"
    - "Dashboard shows payment-failed banner when payment_failed_at is set in D1"
    - "isUserPro() reads from D1 (not KV) in all callers (card.ts, dashboard.ts)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Stripe subscription integration — dashboard UI, billing flows, and PRO gating.

Purpose: Phase 18 involves payment handling that must be visually and functionally correct before shipping. Human eyes confirm the UI is right.
Output: Approval or issue list for gap closure
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-stripe-subscriptions/18-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Stripe subscription integration:
    1. Stripe SDK + D1 schema + subscription helpers (Plan 01)
    2. Webhook handler for subscription lifecycle events (Plan 02)
    3. Billing routes — checkout, portal, welcome page with GitHub OAuth redirect flow (Plan 03)
    4. Dashboard billing UI — PRO badge, payment banner, upgrade prompts, billing section (Plan 04)
  </what-built>
  <how-to-verify>
    1. Open the dashboard at https://shipcard.dev/u/{your-username}/dashboard (or local dev)
    2. Verify FREE user experience:
       - No PRO badge in header
       - BYOT section shows upgrade prompt with feature list and $2/mo + $20/yr links
       - Billing section shows "Free" plan
       - Upgrade links point to /billing/checkout?interval=month and /billing/checkout?interval=year (GET, no forms)
    3. Verify the upgrade prompt design:
       - Feature list is readable and compelling
       - Pricing links are clearly labeled
       - Annual option shows "Save 17%" tag
    4. Click an upgrade link — verify it redirects to GitHub OAuth (github.com/login/oauth/authorize)
    5. Verify PRO user experience (set test subscription in D1):
       - PRO badge appears in header (orange pill)
       - BYOT section is unlocked (no upgrade overlay)
       - Billing section shows "PRO Active", next billing date, "Manage Subscription" link
       - "Manage Subscription" is a plain link to /billing/portal (GET)
    6. Verify payment-failed banner (set payment_failed_at in D1):
       - Red-tinted banner appears below header
       - "Update Payment" link is visible and points to /billing/portal
    7. Check welcome page at /billing/welcome — should show PRO confirmation with feature list
    8. Run `npx tsc --noEmit` in shipcard-worker — should pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human confirms all billing UI elements render correctly and the upgrade/management flows make sense.
</verification>

<success_criteria>
Phase 18 is visually and functionally complete. Human has verified the dashboard billing experience for free, PRO, and payment-failed states.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-subscriptions/18-05-SUMMARY.md`
</output>
