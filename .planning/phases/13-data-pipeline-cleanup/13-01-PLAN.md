---
phase: 13-data-pipeline-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shiplog/src/parser/deduplicator.ts
  - shiplog/src/engine/dailyAggregator.ts
autonomous: true

must_haves:
  truths:
    - "Daily aggregator produces per-project stats (tokens, sessions, messages, cost, toolCalls, thinkingBlocks, models) for each day"
    - "userMessages field reflects actual per-day user message counts instead of hardcoded 0"
    - "Per-project data is keyed by bare directory name (last path segment), not full path"
  artifacts:
    - path: "shiplog/src/parser/deduplicator.ts"
      provides: "userMessagesByDate Map<string, number> from processFile and parseAllFiles"
      contains: "userMessagesByDate"
    - path: "shiplog/src/engine/dailyAggregator.ts"
      provides: "PerProjectDailyStats interface and byProject accumulation"
      contains: "PerProjectDailyStats"
      contains_also: "byProject"
  key_links:
    - from: "shiplog/src/parser/deduplicator.ts"
      to: "shiplog/src/engine/dailyAggregator.ts"
      via: "userMessagesByDate passed to aggregateDaily"
      pattern: "userMessagesByDate"
    - from: "shiplog/src/engine/dailyAggregator.ts"
      to: "DailyStats.byProject"
      via: "ProjectDayAccumulator accumulated per message"
      pattern: "byProject"
---

<objective>
Enrich the daily aggregator with per-project stat breakdowns and fix the userMessages bug.

Purpose: Phase 13's data foundation -- downstream plans (SafeTimeSeries, Worker API, dashboard) all consume DailyStats. Per-project stats enable Phase 15's sort-by-metric feature.
Output: Updated deduplicator returning userMessagesByDate, updated dailyAggregator producing byProject map and real userMessages counts.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-data-pipeline-cleanup/13-RESEARCH.md
@shiplog/src/parser/deduplicator.ts
@shiplog/src/engine/dailyAggregator.ts
@shiplog/src/parser/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add userMessagesByDate to deduplicator</name>
  <files>shiplog/src/parser/deduplicator.ts</files>
  <action>
Modify `processFile()` to track user messages by date:

1. Add a `userMessagesByDate` Map<string, number> inside processFile().
2. In the `isUserEntry(raw)` branch (line 82), extract the date from `raw.timestamp.slice(0, 10)` and increment the map entry for that date.
3. Return `userMessagesByDate` alongside existing `messages` and `userMessages` in the return type.
4. Update the return type annotation: `Promise<{ messages: ParsedMessage[]; userMessages: number; userMessagesByDate: Map<string, number> }>`

Modify `parseAllFiles()` to merge per-file userMessagesByDate maps:

1. Create a merged `userMessagesByDate = new Map<string, number>()` at the top of parseAllFiles.
2. After each `processFile()` call, merge `fileResult.userMessagesByDate` into the merged map (additive).
3. Add `userMessagesByDate` to the `ParseResult` interface.
4. Return it in the result object.

Important: Do NOT change any `ParsedMessage` fields. The date map is a separate data channel alongside messages.
  </action>
  <verify>Run `npx tsc --noEmit` from shiplog/ directory -- no type errors. Grep for "userMessagesByDate" to confirm it appears in processFile return, ParseResult interface, and parseAllFiles merge loop.</verify>
  <done>processFile returns userMessagesByDate map, parseAllFiles merges and returns it in ParseResult, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-project breakdown and userMessages fix to dailyAggregator</name>
  <files>shiplog/src/engine/dailyAggregator.ts</files>
  <action>
1. Define a new exported interface `PerProjectDailyStats`:
```typescript
export interface PerProjectDailyStats {
  sessions: number;
  messages: number;
  userMessages: number;
  tokens: { input: number; output: number; cacheCreate: number; cacheRead: number };
  costCents: number;
  toolCalls: Record<string, number>;
  thinkingBlocks: number;
  models: Record<string, number>;
}
```

2. Add `byProject?: Record<string, PerProjectDailyStats>` to the `DailyStats` interface. Make it optional so existing consumers don't break.

3. Define an internal `ProjectDayAccumulator` interface (mirrors DayAccumulator but per-project):
```typescript
interface ProjectDayAccumulator {
  sessions: Set<string>;
  messages: number;
  tokens: TokenCounts;
  costRaw: number;
  toolCalls: Record<string, number>;
  thinkingBlocks: number;
  models: Record<string, number>;
}
```

4. Add `byProject: Map<string, ProjectDayAccumulator>` to the `DayAccumulator` interface. Initialize it as `new Map()` in the bucket creation block.

5. In the message loop, after accumulating into the day bucket, also accumulate into the per-project sub-bucket:
   - Key: `projectNameFromCwd(msg.cwd)` (existing helper)
   - Get-or-init pattern: if project key doesn't exist in `bucket.byProject`, create a fresh ProjectDayAccumulator
   - Accumulate sessions, messages, tokens, cost, toolCalls, thinkingBlocks, models (same logic as the day-level accumulator)

6. Change `aggregateDaily()` signature to accept an optional `userMessagesByDate?: Map<string, number>` parameter.

7. In the conversion loop (Map -> DailyStats[]), set `userMessages` from the map:
   `userMessages: userMessagesByDate?.get(date) ?? 0`
   Remove the TODO comment.

8. In the conversion loop, convert `bucket.byProject` Map to a `Record<string, PerProjectDailyStats>`:
   - For each project accumulator, convert sessions Set to .size, round costRaw to costCents
   - Set `userMessages: 0` for per-project (we don't have per-project user message data -- user messages aren't associated with a project in the JSONL)
   - Only include `byProject` in DailyStats if the map is non-empty

Important: Keep the existing `projects: string[]` field on DailyStats -- it's used by existing consumers. `byProject` is additive.
  </action>
  <verify>Run `npx tsc --noEmit` from shiplog/ directory -- no type errors. Run `npx tsc --noEmit` from shiplog-worker/ too (types.ts imports nothing from shiplog but should still compile). Check that DailyStats has both `projects` and `byProject` fields.</verify>
  <done>PerProjectDailyStats interface exported, DailyStats has optional byProject field, aggregateDaily accepts userMessagesByDate and populates both fields correctly, all TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire userMessagesByDate through runEngineFull to sync command</name>
  <files>shiplog/src/index.ts, shiplog/src/cli/commands/sync.ts</files>
  <action>
1. In `shiplog/src/index.ts` (`runEngineFull`):
   - The `parseResult` from `parseAllFiles()` now includes `userMessagesByDate`.
   - Pass it through to the return value. Update `EngineFullResult` to include `userMessagesByDate: Map<string, number>`.
   - When date filtering is applied, filter the userMessagesByDate map to only include dates within the since/until range (same logic as message filtering).

2. In `shiplog/src/cli/commands/sync.ts` (`runSync`):
   - Destructure `userMessagesByDate` from `runEngineFull()` result.
   - Pass it to `aggregateDaily(messages, pricing, userMessagesByDate)` -- the new optional third parameter.

This wires the full data flow: JSONL parse -> userMessagesByDate -> aggregateDaily -> DailyStats.userMessages.
  </action>
  <verify>Run `npx tsc --noEmit` from shiplog/ directory -- clean compile. Grep for `userMessagesByDate` across all files to confirm the full chain: deduplicator -> index.ts -> sync.ts -> dailyAggregator.</verify>
  <done>userMessagesByDate flows from parser through engine to aggregator. aggregateDaily receives real user message counts per date. TypeScript compiles clean across the full chain.</done>
</task>

</tasks>

<verification>
1. `cd shiplog && npx tsc --noEmit` -- zero errors
2. Grep confirms `byProject` appears in DailyStats interface and aggregateDaily conversion
3. Grep confirms `userMessagesByDate` flows: deduplicator.ts -> index.ts -> sync.ts -> dailyAggregator.ts
4. Grep confirms `PerProjectDailyStats` is exported from dailyAggregator.ts
5. The `userMessages: 0` TODO comment is removed from dailyAggregator.ts
</verification>

<success_criteria>
- DailyStats includes optional `byProject: Record<string, PerProjectDailyStats>` with sessions, messages, tokens, costCents, toolCalls, thinkingBlocks, models per project per day
- userMessages is populated from actual UserEntry timestamps, not hardcoded to 0
- Per-project keys are bare directory names (last path segment)
- All existing consumers of DailyStats continue to work (byProject is optional)
- TypeScript compiles clean in shiplog/
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-pipeline-cleanup/13-01-SUMMARY.md`
</output>
