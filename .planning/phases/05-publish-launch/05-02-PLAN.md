---
phase: 05-publish-launch
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - shiplog/package.json
  - LICENSE
autonomous: true

must_haves:
  truths:
    - "npm run build in shiplog/ completes with zero errors"
    - "npm pack --dry-run shows dist/, data/pricing-snapshot.json, and LICENSE — no src/ or node_modules/"
    - "LICENSE file exists at project root with MIT text"
    - "Built bin files have shebangs and executable bits"
  artifacts:
    - path: "shiplog/dist/cli/index.js"
      provides: "CLI entry point with shebang"
      contains: "#!/usr/bin/env node"
    - path: "shiplog/dist/mcp/server.js"
      provides: "MCP server entry point with shebang"
      contains: "#!/usr/bin/env node"
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
  key_links:
    - from: "shiplog/package.json"
      to: "shiplog/dist/"
      via: "files field includes dist/"
      pattern: '"dist/"'
    - from: "shiplog/package.json"
      to: "shiplog/data/"
      via: "files field includes data/"
      pattern: '"data/"'
---

<objective>
Verify the renamed package builds cleanly, inspect the npm tarball contents, and add the MIT LICENSE file.

Purpose: Catch any rename breakage before publish. Confirm the tarball contains exactly what users need (dist/, data/, LICENSE) and nothing they don't (src/, node_modules/). PUB-03 requires MIT license.

Output: Clean build, verified tarball contents, LICENSE file at project root.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-publish-launch/05-CONTEXT.md
@.planning/phases/05-publish-launch/05-RESEARCH.md
@.planning/phases/05-publish-launch/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE file and verify build + tarball</name>
  <files>
    LICENSE
    shiplog/package.json
  </files>
  <action>
    **Step 1 — LICENSE file:**
    Create `LICENSE` at project root with standard MIT License text. Copyright holder: Jaime Aleman. Year: 2025.

    Also add `"license": "MIT"` to shiplog/package.json if not already present (it should be from research spec, but verify).

    **Step 2 — Build verification:**
    Run `cd shiplog && npm run build` to compile TypeScript. This must complete with zero errors after the rename in Plan 01. If there are import errors or type errors, they indicate incomplete rename — fix them.

    After build, verify:
    - `dist/cli/index.js` exists and starts with `#!/usr/bin/env node`
    - `dist/mcp/server.js` exists and starts with `#!/usr/bin/env node`
    - Both files have executable permission (chmod 755 is in the build script)

    **Step 3 — Tarball inspection:**
    Run `cd shiplog && npm pack --dry-run` and inspect the output.

    Expected contents (MUST include):
    - `package.json`
    - `dist/cli/index.js`
    - `dist/mcp/server.js`
    - `dist/index.js`
    - `data/pricing-snapshot.json`
    - Various other dist/ files (types, engine, card modules)

    Must NOT include:
    - `src/` (TypeScript source — not needed in published package)
    - `node_modules/`
    - `tsconfig.json`
    - `.gitignore`

    If `data/pricing-snapshot.json` is missing from the dry run output, the `files` field in package.json needs updating to include `"data/"`.

    **Step 4 — Package.json final check:**
    Verify these fields are correct in shiplog/package.json:
    - `"name": "shipcard"` (from Plan 01)
    - `"version": "0.1.0"`
    - `"type": "module"`
    - `"bin"` has both `"shipcard"` and `"shipcard-mcp"` entries
    - `"files"` includes both `"dist/"` and `"data/"`
    - `"engines"` specifies `"node": ">=22.0.0"`
    - `"license": "MIT"`
  </action>
  <verify>
    Run: `cd shiplog && npm run build` — exits 0
    Run: `head -1 shiplog/dist/cli/index.js` — shows shebang
    Run: `ls -la shiplog/dist/cli/index.js | cut -c1-10` — shows -rwxr-xr-x
    Run: `cd shiplog && npm pack --dry-run 2>&1 | grep -E "(data/|dist/cli|dist/mcp)"` — shows data/ and bin files
    Run: `cat LICENSE | head -1` — shows MIT License
  </verify>
  <done>
    Build compiles cleanly after rename. Both bin entry files have shebangs and executable bits. npm pack dry run confirms dist/ and data/ are included, src/ is excluded. LICENSE file exists with MIT text. Package.json has all required fields.
  </done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 in shiplog/
- `npm pack --dry-run` output includes data/pricing-snapshot.json
- `npm pack --dry-run` output does NOT include src/
- LICENSE file exists at project root
- shiplog/package.json has license: MIT
</verification>

<success_criteria>
- Clean build with zero TypeScript errors (proves rename was complete)
- Tarball contains exactly: dist/, data/, package.json, LICENSE
- Both bin files executable with shebangs
- PUB-03 (MIT license) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-publish-launch/05-02-SUMMARY.md`
</output>
