---
phase: 05-publish-launch
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - USAGE.md
  - STYLES.md
autonomous: true

must_haves:
  truths:
    - "README.md has a live card embed using ![](https://shipcard.dev/u/...) syntax"
    - "README.md has a copy-paste MCP config block for Claude Code"
    - "README.md has a Quick Start section with 3-4 commands"
    - "USAGE.md has full CLI command reference and MCP tool reference"
    - "STYLES.md has a visual gallery of all layout x theme x style combinations"
  artifacts:
    - path: "README.md"
      provides: "Product landing page in markdown"
      min_lines: 40
    - path: "USAGE.md"
      provides: "Full CLI and MCP reference"
      min_lines: 60
    - path: "STYLES.md"
      provides: "Card style gallery"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: "https://shipcard.dev/u/"
      via: "img embed URL"
      pattern: "shipcard\\.dev/u/"
    - from: "README.md"
      to: "USAGE.md"
      via: "markdown link"
      pattern: "USAGE\\.md"
    - from: "README.md"
      to: "STYLES.md"
      via: "markdown link"
      pattern: "STYLES\\.md"
---

<objective>
Write the README that sells ShipCard in 30 seconds, plus USAGE.md (full reference) and STYLES.md (card gallery).

Purpose: PUB-04 requires README with card embed example, MCP config snippet, and CLI usage. The README is the product's landing page — it must demo itself with a live card, show the quick start, and get out of the way. Detailed docs go in separate files.

Output: Three markdown files at project root — README.md, USAGE.md, STYLES.md.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-publish-launch/05-CONTEXT.md
@.planning/phases/05-publish-launch/05-RESEARCH.md
@.planning/phases/05-publish-launch/05-01-SUMMARY.md
@shiplog/src/cli/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
    Create README.md at project root following the structure from CONTEXT.md. The README must feel hand-crafted — short, direct, no filler.

    **Structure:**

    1. **Hero section** (top):
       - `# shipcard`
       - Live card embed: `<img src="https://shipcard.dev/u/jjaimealeman" alt="ShipCard" width="495" />`
       - One-line tagline: something like "See what you shipped. Share the proof." or similar (Claude's discretion on exact wording, but keep it short and punchy — NOT "AI-powered analytics dashboard for agentic developers")
       - Brief 1-2 sentence description of what it does: reads Claude Code JSONL files, shows sessions/tokens/cost/models, generates embeddable SVG cards

    2. **Quick Start** section:
       ```
       npm install -g shipcard
       shipcard summary
       shipcard card --preview
       ```
       Maybe a 4th command showing sync: `shipcard login && shipcard sync`

    3. **Embed Your Card** section:
       Show the markdown syntax:
       ```markdown
       ![ShipCard](https://shipcard.dev/u/YOUR_USERNAME)
       ```
       Mention the web configurator at shipcard.dev for customizing theme/layout/style

    4. **MCP Config** section:
       Copy-paste JSON block for Claude Code settings:
       ```json
       {
         "mcpServers": {
           "shipcard": {
             "command": "npx",
             "args": ["-y", "shipcard-mcp"]
           }
         }
       }
       ```
       One sentence explaining what this enables (ask Claude about your coding stats)

    5. **CLI** section:
       Brief table or list of main commands (summary, costs, card, login, sync)
       Link to USAGE.md for full reference

    6. **Footer** section:
       - Link to STYLES.md for card gallery
       - Link to shipcard.dev configurator
       - MIT License mention

    **Tone guidelines:**
    - No "Welcome to..." or "Getting started with..."
    - No badge soup at the top
    - No "Features" section with bullet point lists of buzzwords
    - Let the live card demo the product — that's the hero
    - Every section earns its place or gets cut
  </action>
  <verify>
    Run: `grep -c "shipcard.dev/u/" README.md` — at least 2 (hero embed + embed section)
    Run: `grep "mcpServers" README.md` — MCP config block present
    Run: `grep "npm install" README.md` — install command present
    Run: `wc -l README.md` — between 40 and 120 lines (concise, not bloated)
  </verify>
  <done>
    README.md exists with live card embed, quick start commands, embed syntax, MCP config snippet, and CLI overview. Under 120 lines. No filler.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write USAGE.md and STYLES.md</name>
  <files>USAGE.md, STYLES.md</files>
  <action>
    **USAGE.md — Full CLI + MCP reference:**

    Structure:
    1. **CLI Commands** header
       - `shipcard summary` — what it shows, flags (--json, --since, --until)
       - `shipcard costs` — what it shows, flags (--json, --since, --until)
       - `shipcard card` — what it shows, flags (--layout, --style, --theme, --hide, --hero-stat, --preview, --embed, -o, --json)
       - `shipcard login` — GitHub device flow auth
       - `shipcard sync` — push stats to shipcard.dev
       - `shipcard --help` and `shipcard --version`

    2. **MCP Tools** header
       - `shipcard:summary` — description, parameters, example response
       - `shipcard:costs` — description, parameters, example response
       - `shipcard:card` — description, parameters, example response

    3. **Configuration** header
       - Display config: `~/.shipcard.json` (color, mode)
       - Auth config: `~/.shipcard/config.json` (token, username, workerUrl)

    For each command/tool, show the actual flags/params. Reference the source code in shiplog/src/cli/index.ts and shiplog/src/cli/commands/ for the exact flag names and descriptions.

    **STYLES.md — Card gallery:**

    Structure:
    1. Brief intro: "ShipCard supports 3 layouts, 3 styles, and 2 themes — 18 combinations."
    2. **Layouts section:** Show each layout with an img embed URL using query params
       - Classic: `![](https://shipcard.dev/u/jjaimealeman?layout=classic)`
       - Compact: `![](https://shipcard.dev/u/jjaimealeman?layout=compact)`
       - Hero: `![](https://shipcard.dev/u/jjaimealeman?layout=hero)`
    3. **Themes section:** dark vs light examples
    4. **Styles section:** github, branded, minimal examples
    5. Note: "Customize at shipcard.dev" with link

    The images will render once the Worker is deployed. Before that, they show placeholders. That's fine — the README structure is what matters now.

    Use `<img>` tags with width="495" for consistent rendering.
  </action>
  <verify>
    Run: `wc -l USAGE.md` — at least 60 lines (comprehensive reference)
    Run: `grep -c "shipcard:" USAGE.md` — at least 3 (three MCP tools)
    Run: `grep "layout=" STYLES.md` — shows layout query params
    Run: `test -f STYLES.md && echo "exists"` — file exists
  </verify>
  <done>
    USAGE.md has complete CLI command reference (all flags) and MCP tool reference (all three tools). STYLES.md has visual gallery with img embeds for layout/theme/style combinations. Both files reference shipcard.dev URLs.
  </done>
</task>

</tasks>

<verification>
- README.md contains live card embed with shipcard.dev/u/ URL
- README.md contains MCP config JSON block
- README.md contains npm install command
- README.md links to USAGE.md and STYLES.md
- USAGE.md covers all 5 CLI commands and 3 MCP tools
- STYLES.md shows layout/theme/style gallery with img embeds
- No file references "shiplog" anywhere
</verification>

<success_criteria>
- PUB-04 satisfied: README has card embed example, MCP config snippet, CLI usage
- README is under 120 lines and sells the product in 30 seconds
- USAGE.md is a complete reference for both CLI and MCP interfaces
- STYLES.md showcases all card style combinations
- All URLs use shipcard.dev domain
</success_criteria>

<output>
After completion, create `.planning/phases/05-publish-launch/05-03-SUMMARY.md`
</output>
