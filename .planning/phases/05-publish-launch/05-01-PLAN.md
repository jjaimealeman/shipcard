---
phase: 05-publish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipcard/package.json
  - shipcard/src/cli/config.ts
  - shipcard/src/cli/index.ts
  - shipcard/src/cli/commands/login.ts
  - shipcard/src/cli/commands/sync.ts
  - shipcard/src/cli/commands/card.ts
  - shipcard/src/cli/commands/summary.ts
  - shipcard/src/cli/commands/costs.ts
  - shipcard/src/cli/safestats.ts
  - shipcard/src/engine/cost.ts
  - shipcard/src/mcp/server.ts
  - shipcard/src/mcp/tools/summary.ts
  - shipcard/src/mcp/tools/costs.ts
  - shipcard/src/mcp/tools/card.ts
  - shipcard-worker/package.json
  - shipcard-worker/wrangler.jsonc
  - shipcard-worker/src/index.ts
  - shipcard-worker/src/kv.ts
  - shipcard-worker/src/routes/auth.ts
  - shipcard-worker/src/routes/card.ts
  - shipcard-worker/src/routes/configure.ts
  - shipcard-worker/src/svg/index.ts
autonomous: true

must_haves:
  truths:
    - "Zero occurrences of 'shiplog' remain in any .ts, .json, or .jsonc source file (case-insensitive grep returns empty)"
    - "Config paths reference ~/.shipcard/ and ~/.shipcard.json, not ~/.shipcard/"
    - "Worker card route serves at /u/:username, not /card/:username"
    - "wrangler.jsonc has name 'shipcard' and custom_domain route for shipcard.dev"
    - "package.json bin entries are 'shipcard' and 'shipcard-mcp'"
  artifacts:
    - path: "shipcard/package.json"
      provides: "npm package config with shipcard name"
      contains: '"name": "shipcard"'
    - path: "shipcard-worker/wrangler.jsonc"
      provides: "Worker deploy config with custom domain"
      contains: "shipcard.dev"
    - path: "shipcard-worker/src/index.ts"
      provides: "Worker entry with /u route prefix"
      contains: '"/u"'
  key_links:
    - from: "shipcard/src/cli/config.ts"
      to: "~/.shipcard/config.json"
      via: "path constant"
      pattern: "\\.shipcard"
    - from: "shipcard-worker/src/index.ts"
      to: "shipcard-worker/src/routes/card.ts"
      via: "app.route('/u', cardRoutes)"
      pattern: 'route.*"/u"'
---

<objective>
Rename the entire codebase from "shiplog" to "shipcard" and update the Worker route from /card/:username to /u/:username.

Purpose: The npm name "shiplog" is taken. The product ships as "shipcard" with domain shipcard.dev. Every user-facing string, config path, bin entry, Worker name, and route must reflect this before build/publish.

Output: All source files, package.json files, and wrangler.jsonc updated with zero remaining "shiplog" references. Worker routes updated to /u/:username pattern.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-publish-launch/05-CONTEXT.md
@.planning/phases/05-publish-launch/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all shiplog references to shipcard across source files</name>
  <files>
    shipcard/package.json
    shipcard/src/cli/config.ts
    shipcard/src/cli/index.ts
    shipcard/src/cli/commands/login.ts
    shipcard/src/cli/commands/sync.ts
    shipcard/src/cli/commands/card.ts
    shipcard/src/cli/commands/summary.ts
    shipcard/src/cli/commands/costs.ts
    shipcard/src/cli/safestats.ts
    shipcard/src/engine/cost.ts
    shipcard/src/mcp/server.ts
    shipcard/src/mcp/tools/summary.ts
    shipcard/src/mcp/tools/costs.ts
    shipcard/src/mcp/tools/card.ts
  </files>
  <action>
    Rename all occurrences of "shiplog" to "shipcard" across the shipcard/ package. This is a mechanical find-and-replace with these specific mappings:

    **package.json:**
    - `"name": "shiplog"` -> `"name": "shipcard"`
    - `"shiplog"` bin entry -> `"shipcard"`
    - `"shiplog-mcp"` bin entry -> `"shipcard-mcp"`
    - description: update "Analytics layer" to mention shipcard

    **src/cli/config.ts (highest density — 15 occurrences):**
    - `~/.shiplog.json` -> `~/.shipcard.json`
    - `~/.shipcard/config.json` -> `~/.shipcard/config.json`
    - `~/.shipcard/` directory path -> `~/.shipcard/`
    - `SHIPLOG_DIR` constant -> `SHIPCARD_DIR`
    - `shiplog.workers.dev` default URL -> `shipcard.dev`
    - All "ShipLog" in comments/jsdoc -> "ShipCard"

    **src/cli/index.ts (14 occurrences):**
    - Help text: "shiplog" command references -> "shipcard"
    - Version display: "shiplog" -> "shipcard"
    - All subcommand examples in help text

    **src/cli/commands/login.ts (8 occurrences):**
    - `shiplog login` references in comments/messages -> `shipcard login`
    - `shiplog.workers.dev` callback URL reference -> `shipcard.dev`
    - `User-Agent: shiplog-cli/1.0` -> `User-Agent: shipcard-cli/1.0`
    - `SHIPLOG_GITHUB_CLIENT_ID` placeholder reference -> `SHIPCARD_GITHUB_CLIENT_ID`

    **src/cli/commands/sync.ts (9 occurrences):**
    - `shiplog sync` references -> `shipcard sync`
    - `shiplog login` references in error messages -> `shipcard login`
    - Any "ShipLog" in comments -> "ShipCard"

    **Remaining files (1-3 occurrences each):**
    - card.ts, summary.ts, costs.ts, safestats.ts: update string literals and comments
    - engine/cost.ts: update comments referencing "shiplog"
    - mcp/server.ts: update server name string
    - mcp/tools/*.ts: update tool name prefixes from "shiplog:" to "shipcard:"

    **CRITICAL verification after rename:** Run `grep -ri "shiplog" shipcard/src/ shipcard/package.json` and confirm ZERO results. Check both lowercase "shiplog" and mixed case "ShipLog".

    Do NOT rename the directory itself (shipcard/ stays as-is on disk for now — the npm package name is what matters).
  </action>
  <verify>
    Run: `grep -ri "shiplog" shipcard/src/ shipcard/package.json`
    Expected: zero output (no remaining references)
    Run: `grep -i "shipcard" shipcard/package.json | head -5`
    Expected: name, bin entries show "shipcard"
  </verify>
  <done>
    Every .ts file in shipcard/src/ and shipcard/package.json contains zero "shiplog" references. All user-facing strings, config paths, bin entries, and tool names reference "shipcard".
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename Worker + update route from /card to /u + add custom domain</name>
  <files>
    shipcard-worker/package.json
    shipcard-worker/wrangler.jsonc
    shipcard-worker/src/index.ts
    shipcard-worker/src/kv.ts
    shipcard-worker/src/routes/auth.ts
    shipcard-worker/src/routes/card.ts
    shipcard-worker/src/routes/configure.ts
    shipcard-worker/src/svg/index.ts
  </files>
  <action>
    Two changes in the Worker: (1) rename shiplog -> shipcard everywhere, and (2) change the card route prefix from /card to /u.

    **wrangler.jsonc:**
    - `"name": "shiplog-worker"` -> `"name": "shipcard"`
    - Add routes array for custom domain:
      ```jsonc
      "routes": [
        {
          "pattern": "shipcard.dev",
          "custom_domain": true
        }
      ]
      ```
    - Keep placeholder KV namespace IDs as-is (user will fill in during deploy)

    **src/index.ts:**
    - Health check response: `"shiplog-worker"` -> `"shipcard"`
    - Route mount: `app.route("/card", cardRoutes)` -> `app.route("/u", cardRoutes)`
    - Update JSDoc comment: `GET /card/:username` -> `GET /u/:username`
    - All "ShipLog" in comments -> "ShipCard"

    **src/routes/card.ts:**
    - Update JSDoc: `GET /card/:username` -> `GET /u/:username`
    - "ShipLog SVG card endpoint" -> "ShipCard SVG card endpoint"

    **src/routes/configure.ts:**
    - Update any "shiplog" references in the configurator HTML template
    - Update any URLs pointing to `/card/` to use `/u/`

    **src/kv.ts, src/routes/auth.ts, src/svg/index.ts:**
    - Update any "shiplog" references in comments or string literals

    **package.json (Worker):**
    - Update name field if present

    Do NOT rename the shipcard-worker/ directory itself.
  </action>
  <verify>
    Run: `grep -ri "shiplog" shipcard-worker/src/ shipcard-worker/wrangler.jsonc shipcard-worker/package.json`
    Expected: zero output
    Run: `grep "/card" shipcard-worker/src/index.ts`
    Expected: zero output (route is now /u)
    Run: `grep "shipcard.dev" shipcard-worker/wrangler.jsonc`
    Expected: shows custom_domain route entry
  </verify>
  <done>
    Worker source has zero "shiplog" references. Card route is mounted at /u (so cards serve at /u/:username). wrangler.jsonc has name "shipcard" and custom_domain route for shipcard.dev.
  </done>
</task>

</tasks>

<verification>
- `grep -ri "shiplog" shipcard/src/ shipcard/package.json shipcard-worker/src/ shipcard-worker/wrangler.jsonc shipcard-worker/package.json` returns zero results
- `grep -c "shipcard" shipcard/package.json` returns multiple matches (name, bin entries)
- `grep "route.*\"/u\"" shipcard-worker/src/index.ts` confirms /u mount
- `grep "custom_domain" shipcard-worker/wrangler.jsonc` confirms domain config
</verification>

<success_criteria>
- Zero "shiplog" references remain in any source file across both packages
- package.json name is "shipcard" with bin entries "shipcard" and "shipcard-mcp"
- Worker serves cards at /u/:username path
- wrangler.jsonc configured for shipcard.dev custom domain
- MCP tool names use "shipcard:" prefix
- Config paths point to ~/.shipcard/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/05-publish-launch/05-01-SUMMARY.md`
</output>
