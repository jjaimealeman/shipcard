---
phase: 12-polish-community
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipcard-worker/src/svg/index.ts
  - shipcard-worker/src/svg/layouts/classic.ts
  - shipcard-worker/src/svg/layouts/compact.ts
  - shipcard-worker/src/svg/layouts/hero.ts
  - shipcard/src/card/index.ts
  - shipcard/src/card/layouts/classic.ts
  - shipcard/src/card/layouts/compact.ts
  - shipcard/src/card/layouts/hero.ts
  - shipcard-worker/src/routes/dashboard.ts
autonomous: true

must_haves:
  truths:
    - "SVG card footer reads 'Get yours at shipcard.dev' right-aligned"
    - "Footer appears on all 3 layouts (classic, compact, hero) in both worker and local tool"
    - "Dashboard shows friendly empty state for users with no data"
    - "Dashboard shows error banner when fetch fails"
  artifacts:
    - path: "shipcard-worker/src/svg/index.ts"
      provides: "Footer text change"
      contains: "Get yours at shipcard.dev"
    - path: "shipcard/src/card/index.ts"
      provides: "Footer text change (local tool)"
      contains: "Get yours at shipcard.dev"
    - path: "shipcard-worker/src/routes/dashboard.ts"
      provides: "Empty and error state HTML"
      contains: "empty-state"
  key_links:
    - from: "shipcard-worker/src/svg/index.ts"
      to: "shipcard-worker/src/svg/layouts/*.ts"
      via: "data.footer passed to layout renderers"
      pattern: "footer.*Get yours"
    - from: "shipcard-worker/src/routes/dashboard.ts"
      to: "Alpine store notFound/error"
      via: "x-show directives"
      pattern: "x-show.*notFound"
---

<objective>
Add "Get yours at shipcard.dev" promo footer to all SVG card layouts and add empty/error state handling to the dashboard.

Purpose: The footer is the growth engine — every card embed promotes shipcard.dev. Empty/error states make the dashboard production-ready for unknown users.
Output: Updated SVG footer in 8 files (4 worker + 4 local), empty/error state HTML in dashboard.ts
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-community/12-CONTEXT.md
@.planning/phases/12-polish-community/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SVG card promo footer</name>
  <files>
    shipcard-worker/src/svg/index.ts
    shipcard-worker/src/svg/layouts/classic.ts
    shipcard-worker/src/svg/layouts/compact.ts
    shipcard-worker/src/svg/layouts/hero.ts
    shipcard/src/card/index.ts
    shipcard/src/card/layouts/classic.ts
    shipcard/src/card/layouts/compact.ts
    shipcard/src/card/layouts/hero.ts
  </files>
  <action>
    In both `shipcard-worker/src/svg/index.ts` and `shipcard/src/card/index.ts`:
    - Change `footer: "ShipCard"` to `footer: "Get yours at shipcard.dev"`

    In all 6 layout files (3 in worker, 3 in local tool — classic.ts, compact.ts, hero.ts):
    - Change footer `text-anchor` from `"middle"` to `"end"`
    - Change footer `x` position from `CARD_WIDTH / 2` (or equivalent center value) to `CARD_WIDTH - PADDING`
    - Keep `opacity` at 0.5-0.6 (watermark feel)
    - Keep font-size at 10

    Do NOT wrap footer in `<a>` tag — SVG loaded via `<img src>` sandboxes all hyperlinks. Text-only watermark.

    The worker layouts and local layouts are independent copies with the same structure. Apply the same change to both sets.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shiplog && npx tsc --noEmit` to verify local tool types.
    Run `cd /home/jaime/www/_github/SaaS/shiplog-worker && npx tsc --noEmit` to verify worker types.
    Grep for "Get yours at shipcard.dev" across both directories — should appear in 2 index files.
    Grep for `text-anchor="end"` in all 6 layout files.
  </verify>
  <done>All 3 card layouts (classic, compact, hero) in both worker and local tool render "Get yours at shipcard.dev" right-aligned in the footer area.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard empty and error states</name>
  <files>shipcard-worker/src/routes/dashboard.ts</files>
  <action>
    In dashboard.ts, add empty state and error state HTML blocks that use Alpine's existing store booleans:

    1. **Empty state** (no data for this user):
       - Add a div with `x-show="!$store.dashboard.loading && $store.dashboard.notFound"` and class `empty-state`
       - Content: centered message with heading "No data yet" and paragraph "Sync your stats with `shipcard sync` to see your dashboard."
       - Include a link to shipcard.dev for getting started
       - Style: centered vertically in the main content area, muted text, `code` styling for the command

    2. **Error state** (fetch failed):
       - Add a div with `x-show="$store.dashboard.error"` near the top of the page content
       - Content: single-line red/error banner with the error message: `x-text="$store.dashboard.error"`
       - Style: subtle red background (rgba with low alpha to match dark theme), border-left accent, padding

    3. **Hide dashboard content when notFound**:
       - Wrap the existing dashboard content (hero stats, charts grid) with `x-show="!$store.dashboard.loading && !$store.dashboard.notFound && !$store.dashboard.error"`
       - This ensures empty/error state replaces the dashboard, not overlays it

    Add CSS for `.empty-state` in the existing `<style>` block:
    ```css
    .empty-state {
      text-align: center;
      padding: 80px 20px;
      color: var(--text-secondary);
    }
    .empty-state h2 { color: var(--text-primary); margin-bottom: 12px; }
    .empty-state code { background: var(--panel-bg); padding: 2px 8px; border-radius: 4px; }
    .error-banner {
      background: rgba(239, 68, 68, 0.1);
      border-left: 3px solid #ef4444;
      padding: 12px 16px;
      margin-bottom: 16px;
      border-radius: 4px;
      color: #fca5a5;
    }
    ```

    The Alpine store already has `loading`, `notFound`, and `error` properties from Phase 11. No store changes needed.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shiplog-worker && npx tsc --noEmit` to verify types.
    Grep for "empty-state" in dashboard.ts — should find the CSS class and HTML block.
    Grep for "error-banner" in dashboard.ts — should find the CSS class and HTML block.
  </verify>
  <done>Dashboard shows "No data yet" empty state for unknown users (notFound=true) and an error banner for fetch failures (error is set). Normal dashboard content is hidden when either state is active.</done>
</task>

</tasks>

<verification>
- TypeScript compiles in both shipcard/ and shipcard-worker/ without errors
- Footer text "Get yours at shipcard.dev" appears in both SVG renderers
- Footer is right-aligned (text-anchor="end") in all 6 layout files
- Dashboard empty state uses existing Alpine store booleans (no new state needed)
- Error banner styled subtly (not an aggressive modal)
</verification>

<success_criteria>
1. SVG cards from all 3 layouts show "Get yours at shipcard.dev" watermark, right-aligned
2. Dashboard at /u/nonexistent-user/dashboard shows friendly empty state
3. Dashboard handles error state with inline banner
4. Both local tool and worker SVG renderers updated consistently
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish-community/12-01-SUMMARY.md`
</output>
