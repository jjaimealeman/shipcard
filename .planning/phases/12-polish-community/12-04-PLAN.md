---
phase: 12-polish-community
plan: 04
type: execute
wave: 2
depends_on: ["03"]
files_modified:
  - shipcard-worker/src/routes/landing.ts
  - shipcard-worker/src/routes/community.ts
  - shipcard-worker/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Homepage shows 10-row community teaser table with recent members"
    - "Homepage shows 'Serving X cards' counter when count >= 100"
    - "/community route shows full leaderboard with multiple sort categories"
    - "Community tables handle zero users gracefully"
    - "Users without metadata show partial info (username only, dashes for stats)"
  artifacts:
    - path: "shipcard-worker/src/routes/community.ts"
      provides: "Full community leaderboard page"
      contains: "communityRoutes"
    - path: "shipcard-worker/src/routes/landing.ts"
      provides: "Community teaser section + cards-served counter"
      contains: "Recent members"
    - path: "shipcard-worker/src/index.ts"
      provides: "Community route mounting"
      contains: "communityRoutes"
  key_links:
    - from: "landing.ts"
      to: "kv.ts listUsers()"
      via: "Server-side KV read at request time"
      pattern: "listUsers"
    - from: "landing.ts"
      to: "kv.ts getCardsServedCount()"
      via: "Server-side counter read"
      pattern: "getCardsServedCount"
    - from: "community.ts"
      to: "kv.ts listUsers()"
      via: "Server-side KV list for full leaderboard"
      pattern: "listUsers"
    - from: "index.ts"
      to: "community.ts"
      via: "Hono route mounting"
      pattern: "communityRoutes"
---

<objective>
Build community visibility: a teaser table on the homepage and a full /community leaderboard page, both server-rendered from KV metadata.

Purpose: Community features provide social proof ("look who's using ShipCard") and drive engagement. The homepage teaser funnels visitors to the full community page.
Output: New community.ts route, updated landing.ts with teaser + counter, updated index.ts with route mount
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-community/12-CONTEXT.md
@.planning/phases/12-polish-community/12-RESEARCH.md
@.planning/phases/12-polish-community/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage community teaser and cards-served counter</name>
  <files>shipcard-worker/src/routes/landing.ts</files>
  <action>
    Modify the existing landing page route in landing.ts to add a community section:

    1. **Import helpers**: Import `listUsers` and `getCardsServedCount` from `../kv.js`.

    2. **Server-side data fetch**: In the route handler (the `GET /` handler), before returning `c.html()`:
       ```typescript
       const [users, cardsServed] = await Promise.all([
         listUsers(c.env.USER_DATA_KV, 1000),
         getCardsServedCount(c.env.USER_DATA_KV),
       ]);
       ```

    3. **Cards-served counter**: Below the existing hero subtitle text, add a counter element. Only render it if `cardsServed >= 100`:
       ```html
       ${cardsServed >= 100 ? `<p class="cards-served">Serving ${cardsServed.toLocaleString()} cards</p>` : ''}
       ```
       Style: muted text, smaller font, positioned below the subtitle. Not dominant.

    4. **Community teaser section**: Add a new `<section>` after the existing content sections (before the footer). Contents:
       - Heading: "Recent members" with a "View all" link to /community
       - Table with 6 columns: #, Username, Est. Cost, Projects, Sessions, Tokens
       - Show the 10 most recently synced users (sort by `meta.syncedAt` descending, take first 10)
       - Each username links to `/u/{username}/dashboard`
       - Users with null metadata: show username, dashes for stats
       - If zero users: show "Be the first — run `shipcard sync` to join"

    5. **Table styling**: Match the existing landing page dark theme aesthetic:
       - Dark table background matching existing panel colors
       - Subtle borders, no heavy lines
       - Hover state on rows
       - Monospace for number columns

    6. **Sortable columns**: Add simple client-side Alpine.js sorting (the landing page already loads Alpine or can add a minimal inline script). If Alpine is not already on the landing page, use vanilla JS instead — a simple `thead th[data-sort]` click handler that sorts the table rows. Keep it minimal.

    The landing page is a single large template literal. Inject the users JSON into the HTML at render time (server-side). The community teaser table should feel like a living roster per CONTEXT.md.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shiplog-worker && npx tsc --noEmit` to verify types.
    Grep for "Recent members" in landing.ts — should find the section heading.
    Grep for "listUsers" in landing.ts — should find the import and call.
    Grep for "cards-served" or "cards_served" in landing.ts — should find the counter logic.
  </verify>
  <done>Homepage shows a 10-row "Recent members" table with sortable columns. Cards-served counter appears below hero text when threshold met. Zero users shows friendly fallback message.</done>
</task>

<task type="auto">
  <name>Task 2: Full community page at /community</name>
  <files>
    shipcard-worker/src/routes/community.ts
    shipcard-worker/src/index.ts
  </files>
  <action>
    **Create new file `shipcard-worker/src/routes/community.ts`:**

    Follow the same pattern as landing.ts and dashboard.ts — Hono sub-app with `c.html()` returning a large template literal.

    1. **Route**: `GET /` on the community sub-app (mounted at `/community` in index.ts)

    2. **Data fetch**: Same as landing — `listUsers()` from kv.ts, but show ALL users (not just 10).

    3. **Page layout**: Full HTML page with the same dark theme as landing page and dashboard. Include:
       - Header with site nav (logo/name linking to /, link to /community as active)
       - Page title: "Community" with subtitle "See who's shipping with ShipCard"

    4. **Multiple leaderboard tables** (per CONTEXT.md):
       - **Most Recent**: sorted by `meta.syncedAt` descending (default view)
       - **Most Active**: sorted by `meta.totalSessions` descending
       - **Highest Cost**: sorted by `meta.totalCost` descending (parse the string to number for sorting)
       - **Most Sessions**: sorted by `meta.totalSessions` descending

       Implementation: Server-render all users as JSON in a `<script>` tag. Use Alpine.js (load from CDN, same version as dashboard: 3.15.8) with an `x-data` component that holds `users`, `sortBy`, `sortDir`. Tab buttons switch between categories. Table re-sorts client-side.

    5. **Table columns**: Same as landing teaser: #, Username, Est. Cost, Projects, Sessions, Tokens
       - Each username links to `/u/{username}/dashboard`
       - Null metadata: show username + dashes
       - Sortable column headers (click to toggle asc/desc)

    6. **Pagination**: For Phase 12, show all users (up to 1000 from KV list). No pagination needed yet — document as known limitation for >1000 users.

    7. **Styling**: Dark theme matching landing page and dashboard CSS variables. Reuse the same base styles (font families, colors, etc.) from landing.ts. Table styles should match the teaser table on the landing page for visual consistency.

    8. **Footer**: Same footer as landing page (links, copyright).

    **In index.ts:**

    1. Import `communityRoutes` from `./routes/community.js`
    2. Mount: `app.route("/community", communityRoutes);`
    3. Place the mount AFTER landing routes but BEFORE the `/u` routes — it's a top-level route, not a user sub-route.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/SaaS/shiplog-worker && npx tsc --noEmit` to verify types.
    Grep for "communityRoutes" in community.ts and index.ts — should find export and import.
    Grep for "Most Recent" or "Most Active" in community.ts — should find the sort category labels.
    Grep for '/community' in index.ts — should find the route mount.
  </verify>
  <done>Full community page at /community with multiple sort categories (recent, active, cost, sessions). Alpine.js client-side sorting. All users listed with stats from KV metadata. Route mounted in index.ts.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Landing page community teaser loads via server-side KV read (not AJAX)
- Community page loads Alpine.js from CDN for client-side sorting
- Both pages handle zero users gracefully
- Both pages handle null metadata (v1 users without metadata)
- Cards-served counter only shown when >= 100
- Usernames link to dashboard pages
- Route mounting order in index.ts doesn't break existing routes
</verification>

<success_criteria>
1. Homepage shows 10 most recent members in a styled table
2. "Serving X cards" counter appears when threshold met
3. /community shows all users with multiple sort tabs
4. Column sorting works (click header to toggle)
5. Zero users and null metadata both handled gracefully
6. Dark theme consistent with rest of site
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish-community/12-04-SUMMARY.md`
</output>
