---
phase: 02-mcp-cli
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - shiplog/package.json
  - shiplog/docs/mcp-config.md
autonomous: true

must_haves:
  truths:
    - "`npx shiplog` runs the CLI entry point without prior installation"
    - "`npx shiplog summary` outputs analytics (or onboarding message)"
    - "package.json has `bin.shiplog` pointing to `./dist/cli/index.js`"
    - "package.json has `bin.shiplog-mcp` pointing to `./dist/mcp/server.js`"
    - "Build script compiles TypeScript and sets executable permissions on both bin entries"
    - "MCP config snippet exists for copy-paste into Claude Code / Cursor setup"
    - "MCP config uses `-y` flag with npx to prevent stdin prompt corruption"
  artifacts:
    - path: "shiplog/package.json"
      provides: "Dual bin entries, updated build script with chmod"
      contains: "shiplog-mcp"
    - path: "shiplog/docs/mcp-config.md"
      provides: "Copy-paste MCP config for Claude Code and Cursor"
      contains: "mcpServers"
  key_links:
    - from: "shiplog/package.json"
      to: "shiplog/dist/cli/index.js"
      via: "bin.shiplog entry"
      pattern: "\"shiplog\".*dist/cli/index.js"
    - from: "shiplog/package.json"
      to: "shiplog/dist/mcp/server.js"
      via: "bin.shiplog-mcp entry"
      pattern: "\"shiplog-mcp\".*dist/mcp/server.js"
---

<objective>
Wire bin entries in package.json, update the build script to set executable permissions, and create the copy-paste MCP config documentation.

Purpose: Users can run `npx shiplog` for the CLI and configure `npx -y shiplog-mcp` for MCP server access in Claude Code / Cursor.
Output: Updated package.json with bin entries + build script, MCP config doc file.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-cli/02-RESEARCH.md
@shiplog/package.json
@shiplog/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bin entries, build script, and executable permissions</name>
  <files>
    shiplog/package.json
  </files>
  <action>
    Update `shiplog/package.json`:

    1. Add `bin` field with dual entries:
    ```json
    "bin": {
      "shiplog": "./dist/cli/index.js",
      "shiplog-mcp": "./dist/mcp/server.js"
    }
    ```

    2. Update `build` script to compile + set permissions:
    ```json
    "scripts": {
      "build": "tsc && chmod 755 dist/cli/index.js dist/mcp/server.js",
      "typecheck": "tsc --noEmit"
    }
    ```

    3. Add `main` and `exports` fields for the engine API (consumers who import programmatically):
    ```json
    "main": "./dist/index.js",
    "exports": {
      ".": "./dist/index.js"
    }
    ```

    4. Add `files` field for npm publish (Phase 5 will use this):
    ```json
    "files": ["dist/", "data/"]
    ```

    5. Verify the build works:
    ```bash
    cd shiplog && npm run build
    ```

    6. Test CLI invocation:
    ```bash
    node dist/cli/index.js --help
    ```

    7. Test MCP server invocation (quick check — should not crash on startup):
    ```bash
    echo '' | timeout 3 node dist/mcp/server.js 2>&1 || true
    ```
  </action>
  <verify>
    1. `npm run build` completes without error
    2. `ls -la shiplog/dist/cli/index.js` — shows executable permission (755)
    3. `ls -la shiplog/dist/mcp/server.js` — shows executable permission (755)
    4. `node shiplog/dist/cli/index.js --help` — prints help text
    5. `head -1 shiplog/dist/cli/index.js` — shows `#!/usr/bin/env node`
    6. `head -1 shiplog/dist/mcp/server.js` — shows `#!/usr/bin/env node`
    7. `cat shiplog/package.json | grep -A2 '"bin"'` — shows both entries
  </verify>
  <done>
    package.json has dual bin entries. Build script compiles TypeScript and sets executable permissions. Both entry points have shebangs and are executable. `node dist/cli/index.js --help` works.
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP config documentation for Claude Code and Cursor</name>
  <files>
    shiplog/docs/mcp-config.md
  </files>
  <action>
    Create `shiplog/docs/mcp-config.md` with copy-paste MCP configuration:

    Content should include:

    1. **Claude Code configuration** — add to `.mcp.json` in project root or `~/.claude.json` for global:
    ```json
    {
      "mcpServers": {
        "shiplog": {
          "command": "npx",
          "args": ["-y", "shiplog-mcp"]
        }
      }
    }
    ```

    2. **Cursor configuration** — add to `~/.cursor/mcp.json`:
    ```json
    {
      "mcpServers": {
        "shiplog": {
          "command": "npx",
          "args": ["-y", "shiplog-mcp"]
        }
      }
    }
    ```

    3. **After global install** (`npm install -g shiplog`):
    ```json
    {
      "mcpServers": {
        "shiplog": {
          "command": "shiplog-mcp"
        }
      }
    }
    ```

    4. **Available tools** — brief description of each:
    - `shiplog:summary` — sessions, tool calls, models, projects, and estimated cost
    - `shiplog:costs` — cost breakdown by project and model
    - `shiplog:card` — raw analytics data for card generation

    5. **Important note about `-y` flag:** Required for npx to prevent interactive install prompt, which would corrupt the MCP stdio transport.

    Keep it concise — this doc will be integrated into the README in Phase 5.
  </action>
  <verify>
    1. File exists at `shiplog/docs/mcp-config.md`
    2. Contains valid JSON config blocks for Claude Code and Cursor
    3. Contains `-y` flag in npx args
    4. Lists all three tools
  </verify>
  <done>
    MCP config documentation exists with copy-paste snippets for Claude Code and Cursor. The `-y` flag is documented and explained. All three tools are listed with descriptions. Ready for integration into README in Phase 5.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds in `shiplog/`
2. Both `dist/cli/index.js` and `dist/mcp/server.js` are executable (755)
3. `node dist/cli/index.js --help` prints help
4. `node dist/cli/index.js summary` runs (shows data or onboarding message)
5. `node dist/cli/index.js costs --json` outputs valid JSON
6. package.json has `bin.shiplog` and `bin.shiplog-mcp`
7. `docs/mcp-config.md` exists with valid JSON config snippets
8. MCP config includes `-y` flag
</verification>

<success_criteria>
- CLI-06: Package structured for `npx shiplog` invocation with zero configuration
- MCP-05: Copy-paste MCP config for Claude Code / Cursor documented
- Both bin entries are executable after build
- Build script handles chmod automatically
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-cli/02-03-SUMMARY.md`
</output>
