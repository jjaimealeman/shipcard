---
phase: 16-agent-agnostic-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipcard/ (renamed from shiplog/)
  - shipcard-worker/ (renamed from shiplog-worker/)
  - shipcard/package.json
  - shipcard/tsconfig.json
  - shipcard-worker/package.json
  - shipcard-worker/tsconfig.json
  - shipcard-worker/wrangler.jsonc
  - .planning/**/*.md (path references)
autonomous: true

must_haves:
  truths:
    - "The directory shiplog/ no longer exists — it is now shipcard/"
    - "The directory shiplog-worker/ no longer exists — it is now shipcard-worker/"
    - "Running `pnpm build` in shipcard/ succeeds with zero errors"
    - "Running `pnpm build` in shipcard-worker/ succeeds with zero errors"
    - "All internal import paths resolve correctly after the rename"
  artifacts:
    - path: "shipcard/"
      provides: "Renamed local tool directory"
    - path: "shipcard-worker/"
      provides: "Renamed worker directory"
  key_links:
    - from: "shipcard-worker/tsconfig.json"
      to: "shipcard/"
      via: "path references if any cross-package refs exist"
      pattern: "shipcard"
---

<objective>
Rename `shiplog/` to `shipcard/` and `shiplog-worker/` to `shipcard-worker/`, then update every reference across the entire codebase (source, config, and planning docs).

Purpose: Eliminate the legacy "shiplog" naming before architecture work begins, so all new adapter code uses the correct project name from the start.
Output: Two renamed directories with all references updated and builds passing.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-agent-agnostic-architecture/16-CONTEXT.md
@.planning/phases/16-agent-agnostic-architecture/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories and update source/config references</name>
  <files>
    shipcard/ (was shiplog/)
    shipcard-worker/ (was shiplog-worker/)
    shipcard/package.json
    shipcard/tsconfig.json
    shipcard-worker/package.json
    shipcard-worker/tsconfig.json
    shipcard-worker/wrangler.jsonc
    shipcard-worker/src/**/*.ts
  </files>
  <action>
    1. Rename the directories:
       - `mv shiplog shipcard`
       - `mv shiplog-worker shipcard-worker`

    2. Find all `.ts`, `.json`, `.jsonc` files in `shipcard/` and `shipcard-worker/` (excluding `node_modules/` and `dist/`). Replace any occurrences of `shiplog/` with `shipcard/` and `shiplog-worker/` with `shipcard-worker/` in file contents.

    3. Specifically verify these config files are correct after replacement:
       - `shipcard/package.json` — no internal references should mention shiplog
       - `shipcard-worker/wrangler.jsonc` — check build paths, KV namespace refs
       - `shipcard-worker/tsconfig.json` — check paths/references if they cross-reference the local tool

    4. Clean stale build artifacts: `rm -rf shipcard/dist shipcard-worker/dist` (list as cleanup if sandbox-blocked)

    5. Rebuild both packages to verify:
       - `cd shipcard && pnpm build`
       - `cd shipcard-worker && pnpm build`

    Both builds must complete with zero errors.
  </action>
  <verify>
    - `ls shipcard/src/index.ts` exists
    - `ls shipcard-worker/src/index.ts` exists
    - `ls shiplog/ 2>/dev/null` returns nothing (directory gone)
    - `ls shiplog-worker/ 2>/dev/null` returns nothing (directory gone)
    - `cd shipcard && pnpm build` exits 0
    - `cd shipcard-worker && pnpm build` exits 0
    - `grep -r 'shiplog/' shipcard/src/ --include='*.ts' | wc -l` returns 0
    - `grep -r 'shiplog/' shipcard-worker/src/ --include='*.ts' | wc -l` returns 0
  </verify>
  <done>Both directories renamed, all source references updated, both packages build cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning doc references</name>
  <files>
    .planning/**/*.md
    CLAUDE.md
  </files>
  <action>
    1. Find all `.md` files in `.planning/` (recursively) and in the project root that reference `shiplog/` or `shiplog-worker/`.

    2. Replace `shiplog/` with `shipcard/` and `shiplog-worker/` with `shipcard-worker/` in all matches.

    3. Be careful NOT to replace:
       - The word "shiplog" when it appears as a standalone reference to the old project name in historical context (e.g., "renamed from shiplog" is fine to leave)
       - npm package names like `@jjaimealeman/shipcard` (these are already correct)

    4. Focus on path references like `shiplog/src/parser/schema.ts` → `shipcard/src/parser/schema.ts`.

    5. Verify with a count check:
       - Count `shiplog/` references before (from grep) and after (should be near zero, with only historical mentions remaining)
  </action>
  <verify>
    - `grep -r 'shiplog/src/' .planning/ | wc -l` returns 0
    - `grep -r 'shiplog-worker/src/' .planning/ | wc -l` returns 0
    - CLAUDE.md no longer references `shiplog/` as a current path
  </verify>
  <done>All planning docs and CLAUDE.md updated to reference shipcard/ and shipcard-worker/ paths.</done>
</task>

</tasks>

<verification>
1. `ls shiplog/ 2>/dev/null` — empty (directory gone)
2. `ls shiplog-worker/ 2>/dev/null` — empty (directory gone)
3. `cd shipcard && pnpm build` — exits 0
4. `cd shipcard-worker && pnpm build` — exits 0
5. `grep -rn 'shiplog/' shipcard/ shipcard-worker/ --include='*.ts' --include='*.json' --include='*.jsonc' | grep -v node_modules | wc -l` — returns 0
6. `grep -rn 'shiplog/src/' .planning/ | wc -l` — returns 0
</verification>

<success_criteria>
- Both directories exist at their new names (shipcard/, shipcard-worker/)
- Both packages build with zero TypeScript errors
- Zero stale `shiplog/` path references remain in source code or config files
- Planning docs reference the new paths
</success_criteria>

<output>
After completion, create `.planning/phases/16-agent-agnostic-architecture/16-01-SUMMARY.md`
</output>
