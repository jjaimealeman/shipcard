{
  "name": "shiplog-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — serves ShipLog SVG stats cards at the edge",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "wrangler": "^4.0.0"
  }
}
