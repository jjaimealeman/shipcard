{
  "name": "shipcard-worker",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker — serves ShipCard SVG stats cards at the edge",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "stripe": "^21.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "wrangler": "^4.0.0"
  }
}
